CREATE TABLE public.books(
                      id INT8,
                      isbn VARCHAR(10),
                      title VARCHAR(50),
                      price FLOAT8,
                      store VARCHAR(5),
                      qty INT4,
                      primary key (id)
);
